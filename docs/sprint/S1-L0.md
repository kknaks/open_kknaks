# Sprint 1 — Layer 0 구현 기록

> task.py, config.py, exceptions.py, worker/line_buffer.py

---

## 작업 계획

| # | 작업 | 파일 | 완료 기준 | 상태 |
|---|---|---|---|---|
| 1-1 | 프로젝트 세팅 | pyproject.toml, ruff, mypy | `uv sync` + `uv run ruff check` + `uv run mypy` 통과 | done |
| 1-2 | Task 모델 | task.py | Task, TaskStatus, Priority, TokenUsage, StreamEvent, TaskResult 정의 + 직렬화 테스트 | done |
| 1-2b | ClaudeConfig | config.py | ClaudeConfig 정의 + model_copy 오버라이드 테스트 | done |
| 1-2c | 예외 계층 | exceptions.py | BillingError, ClaudeAuthError 등 계층 정의 | done |
| 1-3 | LineBuffer | worker/line_buffer.py | 바이트 청크 → 줄 조립 + 불완전 UTF-8 처리 테스트 | done |

### 핵심 검증 (L0)

- [x] Task: `model_dump_json()` → `model_validate_json()` 왕복 (datetime, Enum, Optional)
- [x] Task: `exception_type` 필드로 예외 타입 구분 가능
- [x] ClaudeConfig: `model_copy(update={})` 로 오버라이드 동작
- [x] ClaudeConfig: 화이트리스트 외 필드(work_dir, claude_bin) 오버라이드 차단
- [x] LineBuffer: 불완전 UTF-8 시퀀스 버퍼링
- [x] LineBuffer: `\r\n` / `\n` 혼합 처리

### 설계 결정 (확정)

- `datetime.now(timezone.utc)` 사용 (`datetime.utcnow()` 안 씀)
- `ConfigDict(use_enum_values=True)` — Enum → 값으로 직렬화
- `exception_type: str | None` 추가 — RetriesMiddleware 재시도 판단용
- StreamEvent 확장 안 함 (text/cost/retry 유지) — 상태 변경은 Task.status로
- MergedConfig 안 만듦 — `ClaudeConfig.model_copy(update={})` 사용
- 오버라이드 화이트리스트 방식 — work_dir, claude_bin 보안상 불가

---

## 구현 결정사항

- `_uuid4_str()`, `_now_utc()` 헬퍼 함수로 Field default_factory 구현
- `LineBuffer.flush()` 메서드 추가 — 프로세스 종료 시 남은 버퍼 처리용
- `OVERRIDABLE_FIELDS` frozenset으로 화이트리스트 관리
- `merge_task_overrides()` 메서드를 ClaudeConfig에 추가 (Worker._merge_config 대신)
- `_BLOCKED_OVERRIDES` 상수 제거 → OVERRIDABLE_FIELDS만으로 충분

## 발견된 이슈

- 없음

## 인터페이스 변경 이력

- `TimeoutError` → `TaskTimeoutError` 로 이름 변경 (빌트인 충돌 방지)

## 다음 레이어(L1)에 전달할 주의사항

- `from __future__ import annotations` 사용 금지 (pydantic 런타임 타입 필요)
- `contextlib.suppress` 사용 권장 (ruff SIM105 규칙)
- LineBuffer.flush() 반환이 None일 수 있음 — 호출 시 체크 필요
