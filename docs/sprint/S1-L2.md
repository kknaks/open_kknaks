# Sprint 1 — Layer 2 구현 기록

> worker/executor.py, broker/redis.py, broker/lua/

---

## 작업 계획

| # | 작업 | 파일 | 의존 | 완료 기준 | 상태 |
|---|---|---|---|---|---|
| 1-5 | PTY Executor | worker/executor.py | L0, L1 | PTY로 `claude -p` 실행 → stream-json 파싱 → TaskResult | done |
| 1-7 | RedisBroker | broker/redis.py, broker/lua/ | L1 | enqueue/dequeue/ack/nack + Lua 스크립트 6개 | done |
| 1-8 | ClaudeWorker | worker/worker.py | L1, L2 | → S2-L3으로 이동 | moved |
| 1-9 | ClaudeClient | client.py | L2 | → S3-L4로 이동 | moved |
| 1-10 | 테스트 | tests/ | 전부 | unit + integration (fakeredis[lua]) | done |

### 핵심 검증 (L2)

- [x] Executor: idle_timeout(30s) + deadline timeout 듀얼 타임아웃 구현
- [x] Executor: `--output-format stream-json` + `-p` 고정 플래그
- [x] Executor: on_chunk async 콜백 예외 처리 (try/except + warning log)
- [x] RedisBroker: Lua 스크립트 원자성 (enqueue: HSET→ZADD 순서)
- [x] RedisBroker: dequeue 경합 없음 (ZPOPMIN 원자적)
- [ ] RedisBroker: subscribe_chunks XREAD BLOCK 동작 — E2E에서 검증 예정

### 설계 결정 (확정)

- Middleware 체인: before 예외 기반 sequential break, after 역순
- Middleware에 broker 인자 전달 (생성자 주입 아님)
- result()/stream() 둘 다 XREAD BLOCK 기반, 인프라 공유
- Worker._merge_config()에서 ClaudeConfig.model_copy(update={})

---

## 구현 결정사항

- Executor: terminal size 24×200 (cols 넉넉히 — stream-json 줄 잘림 방지)
- Executor: context는 환경변수 `CLAUDE_CONTEXT`로 전달 — stdin pipe 미구현 (WARN)
- Executor: `_build_command()`에서 `--dangerously-skip-permissions` 별도 분기 처리
- RedisBroker: `redis` 파라미터 타입을 `Any`로 — redis.asyncio 타이핑 불안정
- RedisBroker: `_scripts` dict[str, Any]로 — register_script 반환 타입 불명확
- Lua: maintenance.lua는 delayed→main 이동으로 단순화 (zombie worker 감지는 Python에서)
- RedisBroker: `promote_delayed()` 추가 메서드 — AbstractBroker에 없는 Redis 전용
- priority score: `priority * 1e12 + timestamp_ms` — 동일 우선순위 내 FIFO 보장

## 발견된 이슈

### FAIL (리뷰어 지적, 수정 필요)

1. **StreamEvent.type이 str로 느슨함** — `Literal["text", "cost", "retry"]`로 변경 필요
   → `open_kknaks/task.py:47`

2. **Redis 키 인젝션 방어 없음** — `_key()`에서 입력값 검증 안 함. queue_name에 `:` 포함 가능
   → `open_kknaks/broker/redis.py:44`

3. **subscribe_chunks 시그니처 불일치** — base.py(sync def) vs redis.py(async def)
   → 통일 필요

4. **promote_delayed 테스트 불완전** — delay=-1 후 promote → dequeue 까지 검증 필요

5. **broker fixture 리소스 정리 없음** — yield 후 close() 호출 누락
   → `tests/integration/test_broker.py`

### WARN (수정 권장)

- executor.py:86-88 — task.context 환경변수 전달 방식 검증 필요 (Claude CLI 공식 지원 확인)
- executor.py:99 — fork/setsid 사이 race condition 이론적 존재
- broker/redis.py:283 — `_async_sleep_short` 내 불필요한 지연 import
- broker/redis.py:167 — subscribe_chunks 무한 루프, timeout 파라미터 없음

## 인터페이스 변경 이력

- AbstractBroker.emit_before/emit_after 제거 (L1에서 결정)
- RedisBroker.promote_delayed() 추가 — base에 없는 Redis 전용 메서드

## 다음 스프린트(S2-L3)에 전달할 주의사항

### 반드시 S2 시작 전 수정해야 할 것

1. **StreamEvent.type → Literal["text", "cost", "retry"]** 변경
2. **broker fixture에 yield + close() 추가**
3. **subscribe_chunks 시그니처 base/redis 통일**
4. **Redis 키 _key()에 입력값 검증 추가**

### S2-L3 구현 시 주의

- Middleware.before_process: 예외 → 즉시 break, called_middlewares 추적 필수
- Middleware.after_process: 역순 실행, 예외 시에도 모든 MW 호출 (try/except로 감싸기)
- RetriesMiddleware: `no_retry_on = (TaskCancelledError, ClaudeAuthError, BillingError)` 필수
- CostMiddleware: `broker.incr_cost()` 호출 — Redis INCRBYFLOAT 원자적
- Worker._merge_config(): `config.merge_task_overrides()` 사용 (이미 L0에서 구현됨)
- Worker: asyncio.Semaphore(concurrency)로 동시성 제어
- Worker: graceful shutdown 시 in-flight 작업 완료 대기 → timeout → terminate → requeue
