# Sprint 4 — V2: test_app 라이브러리 임포트 검증

> PyPI에서 설치한 라이브러리로 examples/ 동작 확인

---

## 작업 계획

| # | 작업 | 완료 기준 | 상태 |
|---|---|---|---|
| 4-14 | 클린 환경 구성 | 새 venv에서 `pip install open-kknaks[redis]` | |
| 4-15 | examples/ 수정 | 로컬 경로 → PyPI 패키지로 import 변경 | |
| 4-16 | Docker 재검증 | Dockerfile에서 `pip install open-kknaks[redis]` 사용 | |
| 4-17 | 시나리오 재실행 | 01_basic ~ 06_multi_queue PyPI 패키지 기준 | |
| 4-18 | MCP 검증 | Claude Desktop에서 MCP 서버 연동 | |

### 검증 체크리스트

- [ ] 새 venv + `pip install open-kknaks[redis]` → import 동작
- [ ] Worker: PyPI 패키지로 claude 실행 → 결과 반환
- [ ] Docker: PyPI 패키지 기반 `docker compose up` 동작
- [ ] 시나리오 전부 성공 (V1과 동일한 결과)
- [ ] MCP: Claude Desktop → submit_task → 결과 수신
- [ ] CLI: `open-kknaks worker --queues default` 실행

### V1 대비 차이점

| 항목 | V1 (직접 임포트) | V2 (라이브러리) |
|---|---|---|
| 설치 | `pip install -e ".[redis]"` | `pip install open-kknaks[redis]` |
| Dockerfile | `COPY` + `pip install -e` | `pip install open-kknaks[redis]` |
| import 경로 | 동일 | 동일 (패키지명 같음) |
| 목적 | 기능 검증 | 배포 패키지 검증 |

---

## 발견된 이슈

_(검증하면서 기록)_

## 수정 사항

_(라이브러리 코드 수정이 필요했던 것 → 핫픽스 배포 필요 여부)_
